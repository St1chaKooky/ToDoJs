const button = document.querySelector('.buttonAdds');

const listEmpty = document.querySelector('.listEmpty');

const by = document.querySelector('.byList');

const byInfo = by.children;
button.addEventListener('click',buttonClick);
let counter = 0;

function buttonClick(){
    if (counter != 0){

    } else {

    }
}
